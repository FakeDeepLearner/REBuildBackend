services:
  postgres:
    image: postgres:18-alpine3.23
  backend:
    build: .
    env_file:
      - /.env
  redis:
    image: "redis:alpine"
    container_name: "session-data"
    ports:
      - "6379:6379"